[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "patch-impact"
version = "0.2.0"
description = "Reproducibility artifact for patch-grounded software change reports."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "matplotlib>=3.7",
  "numpy>=1.24",
  "pandas>=2.0",
]

[project.scripts]
change-impact = "change_impact.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
